
// Variables
var options = {
  valueNames: [ 'name', 'interest' ]
};
// var listObj = new List ('names', options);
var nameList = new List('names', options);

// Sorts the list in abc-order based on interest
// listObj.sort('interest', { order: "asc" });
